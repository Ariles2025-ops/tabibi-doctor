# Les RPC appelees par le front QUI EXISTENT en base (public.*).
# Reference versionnee, comparee au code par scripts/verifier-rpc.mjs (etage
# structurel) et a pg_proc par le meme script en --base.
# Regeneree par : node scripts/verifier-rpc.mjs --base --ecrire
#
# [14/09/2026] CETTE generation-ci n'a PAS ete faite par cette commande :
# elle exige SUPABASE_ACCESS_TOKEN, et les scripts a secret sont lances par
# Aghiles. La liste de pg_proc a ete lue par le MCP Supabase en LECTURE, et
# l'intersection calculee avec la meme logique, mot pour mot. A confirmer
# par un vrai `--base --ecrire` au prochain passage : si le fichier ne bouge
# pas, la reproduction etait fidele.
#
# [16/09/2026] `soumettre_candidature_medecin` ajoutee A LA MAIN, seule, meme
# discipline : le jeton de `--base --ecrire` est toujours revoque. Lue par le
# MCP Supabase EN LECTURE sur `pg_proc` : elle existe, `p jsonb -> uuid`,
# SECURITY DEFINER, EXECUTE pour anon et authenticated. La migration a ete
# appliquee par le stratege ; je n'ai rien ecrit en base.
#
# [14/09/2026, second passage] `waiting_list_count` ajoutee A LA MAIN, seule.
# Le jeton de `--base --ecrire` a ete REVOQUE (securite) : la regeneration
# complete attend qu'Aghiles en pose un nouveau. Verifie par le MCP en lecture :
# la fonction existe, STABLE, SECURITY DEFINER, search_path fige, GRANT a anon.
#
# ⚠️  CE QUI N'EST DELIBEREMENT PAS FAIT : les quatre RPC d'ordonnance
# (create/update_prescription_draft, request_prescription_signature,
# mark_prescription_delivered) EXISTENT desormais en base, mais restent dans
# ABSENCES_CONNUES et hors de cette reference. Les y porter demande le meme
# passage `--base --ecrire`, et le stratege a demande de NE PAS le forcer.
# Les avoir dans les deux listes serait sans effet sur le controle, mais ce
# fichier dirait alors une chose que personne n'a mesuree avec l'outil prevu.
accept_cabinet_invitation
admin_audit_rebut_count
admin_audit_rebut_list
admin_doctor_doc_paths
admin_validate_doctor
admin_validation_counts
admin_validation_list
admin_validation_total
can_review_doctor
chercher_praticiens
claim_my_doctor_profile
dawini_cancel_alert
dawini_create_alert
dawini_create_request
dawini_expire_old
dawini_get_patient_contact
dawini_pharmacy_stats
dawini_respond
dawini_shortage_by_wilaya
dawini_top_missing
doctor_set_ordre_number
ensure_conversation
generate_api_key_pair
get_available_slots
get_my_cabinets
get_my_doctor_profile
get_patient_medical_data
get_video_session
invite_cabinet_member
mark_video_session_ended
mark_video_session_started
praticien
praticiens_par_ids
record_consent
remove_cabinet_member
revoke_api_key
rotate_api_key
set_video_recording_consent
soumettre_candidature_medecin
stats_publiques
update_my_doctor_profile
upsert_patient_medical_data
waiting_list_count
